#provider: cassandra, postgres, mongo
provider:
  name: postgres
  options:
    dynamic_schema: "no"

#strict, compatibility
mode: "strict"
#mutations, audit, all
generate: "all"

source: "/tmp/input"

#stdout if path is not provided
output: "/tmp/output"
